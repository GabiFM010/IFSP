#include <stdlib.h>
#include <time.h>

int gerar( int min , int max ){
    srand((unsigned)time(NULL));
    return( min + rand() %max );
}

float proximidadePalpite(f){
    sub = n2 - n1;
    return (prox = (sub / n1)*100);
}
