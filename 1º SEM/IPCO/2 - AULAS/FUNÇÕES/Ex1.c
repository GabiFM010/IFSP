#include <stdio.h>

int main(){ // trecho de codigo de nome main, int é o retorno, garante o retorno inteiro
    printf("Hello world!"); //prinf: função do stdio.h, ja feita nessa biblioteca, função de exibição, ja preparada para receber diferentes argumentos
    return(0); //retorno do int main
}
